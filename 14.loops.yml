- name: install packages
  hosts: local
  connection: local
  become: true
  tasks:
  - name: 
    ansible.builtin.package:
      name: {{ item }}
      state: present
    loop:
    - mysql
    - nginx
    - zip   
